# PythonPractiseTutedude
Assignment 1
Task 1 : Arithmetic operations between 2 numbers a and b
used input function to take inputs from the user and used the int() function to convert its data type to integer.
used basic arithmetic operations like "+, -, *, /" and printed them
Task 2: Giving a personalized greeting to a user 
used input function to take inputs from the user about their first and last name and then used the print function to print the greeting.
Assignment 2
Task 1: Checking if a number is even or odd
used input function to take user input and used int() function to convert it's data type to integer.
used % operator to calculate the modulo value of the input integer with 2. (1 means odd, 0 means even)
used if statement to make a condition that dertermined the output. 
Task 2: Sum of numbers 1 to 50
initialized value of sum to be 0
used for loop as the number of iterations are known. start = 1, stop = 51 (so that 50 is included), and stepsize = 1
printed the over all sum after the completion of for loop
